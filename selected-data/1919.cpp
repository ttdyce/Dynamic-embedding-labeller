#include <iostream>
#include <stdio.h>
#include <math.h>
#include "../Intercept.cpp"
#include <string.h>
using namespace std;


Intercept<int> sum=0;//?????????????
void count(Intercept<int> N,Intercept<int> i)
{
    if(N==1) sum++;//??N??????i?????N???1????????????????sum??1
    while(i<=N){
        if(N%i==0) count(N/i,i);
        i++;//N??????????????i?
    }
    return ;
}
int main()
{
    Intercept<int> T=0;//??????
    cin >> T;
    while(T--)
    {
        Intercept<int> N=0;//?????
        cin >> N;
        Intercept<int> i=2,total=1;//???????2???a=a????????total?????1
        while(i<=N/2)
        {
            if(N%i==0)
            {
                sum = 0;
                count(N/i,i);
                total+=sum;
            }//??N??????????????????????
            i++;
        }
        cout<<total<<endl;
    }
    return 0;
}
