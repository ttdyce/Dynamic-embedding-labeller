#include <iostream>
#include <stdio.h>
#include <math.h>
#include "../Intercept.cpp"
#include <string.h>
using namespace std;

//??????sum??????????temp??????
Intercept<int> sum(-1, 3), temp;
void fact(Intercept<int>); //??fact????int?????

int main()
{
	Intercept<int> n, i, k;
	cin >> n;
	for (i = Intercept<int>(0, 2); i < n; i++)
	{
		cin >> k;
		sum = 0;
		temp = 2; //????????????????????????
		fact(k);
		cout << sum << endl;
	}
	return 0;
}

void fact(Intercept<int> k)
{
	if (k == 1) //???????
	{
		sum++;
		return;
	}
	Intercept<int> i; //i?????k???????temp??????????????????????????
	for (i = Intercept<int>(temp, 2); i <= k; i++)
	{
		if (k % i == 0)
		{
			temp = i;	//??temp?
			fact(k / i); //???????
		}
	}
}
