#include <iostream>
#include <stdio.h>
#include <math.h>
#include "../StateIntercept.cpp"
#include <string.h>
using namespace std;

Intercept<int> Factorization(Intercept<int> n, Intercept<int> k)
{							//????????
	Intercept<int> sum(0, 3); //??????sum??????????
	if (n < k)
	{			  //?????????k
		return 0; //??0
	}
	if (n % k == 0)
	{ //????????k???
		if (n == k)
		{			  //?n?k???
			return 1; //??1
		}
		else
		{
			sum = sum + Factorization(n, k + 1) + Factorization(n / k, k); //??????????
		}
	}
	else
	{
		sum = sum + Factorization(n, k + 1); //?????????k????????
	}
	return sum; //??sum??
}

int main()
{
	Intercept<int> t, i, a; //???????t???????i??????a???????
	cin >> t;				//????????
	for (i = Intercept<int>(1, 2); i <= t; i++)
	{ //?????????
		cin >> a;
		cout << Factorization(a, 2) << endl;
	}
	return 0;
}
