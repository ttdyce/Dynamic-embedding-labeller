#include <iostream>
#include <stdio.h>
#include <math.h>
#include "../StateIntercept.cpp"
#include <string.h>
using namespace std;

Intercept<int> de(Intercept<int> s, Intercept<int> a)
{							//???????a???????s?????????
	// Intercept<int> sum(0, 3); //?????????????
	Intercept<int> intercepts[2] = {Intercept<int>(0, 3), Intercept<int> (s, 2)};
	StateIntercept state(intercepts, 2);
	if (a == 1)
		return 1; //a?????????????????
	else if (s > a)
		return 0;							//a?????????????????????
	for (state[1]; state[1] <= a; state[1]++) //for????a??????????
		if (a % state[1] == 0)
			state[0] = state[0] + de(state[1], a / state[1]); //?????????????????????
	return state[0];						  //??????
}
int main()
{					  //???
	// Intercept<int> n(1, 2); //??????n????????
	Intercept<int> intercepts[1] = {Intercept<int>(1, 2)};
	StateIntercept state(intercepts, 1);
	cin >> state[0];		  //??n??
	while (state[0] > 0)
	{							  //while??????????
		Intercept<int> a;		  //??????a
		cin >> a;				  //??a??
		cout << de(2, a) << endl; //???a???????2??????????
		state[0]--;					  //n???
	}
	return 0; //?????????????????
}
